summary:category TEXT, champ TEXT, games INTEGER, wins INTEGER, kda REAL, kills INTEGER, deaths INTEGER, assists INTEGER, cs INTEGER, time INTEGER, gold_earned INTEGER, obj_damage INTEGER, tur_damage INTEGER, champ_damage INTEGER, tot_damage INTEGER, time_ccing INTEGER, PRIMARY KEY (champ, category)
matches:match_id TEXT NOT NULL PRIMARY KEY, has_tgt TEXT, duration TEXT
teams:match_id TEXT NOT NULL, team_id INTEGER NOT NULL PRIMARY KEY, category TEXT, side TEXT, win INTEGER, bans TEXT, total_kills INTEGER, total_deaths INTEGER, baron_kills INTEGER, dragon_kills INTEGER, inhibitor_kills INTEGER, tower_kills INTEGER, first_blood INTEGER, first_tower INTEGER, FOREIGN KEY (match_id) REFERENCES matches (match_id)
summs:team_id INTEGER NOT NULL, category TEXT NOT NULL, player_id TEXT, champ TEXT, role TEXT, won INTEGER, kda REAL, kills INTEGER, deaths INTEGER, assists INTEGER, cs INTEGER, time INTEGER, gold_earned INTEGER, obj_damage INTEGER, tur_damage INTEGER, champ_damage INTEGER, tot_damage INTEGER, time_ccing INTEGER, FOREIGN KEY (team_id) REFERENCES teams (team_id)